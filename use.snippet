	// 生成带logo的二维码
	/**
	 * ${0:变量注释}
	 */
	const r$CURRENT_SECONDS_UNIX = await(() => {
		const p1 = $2;			// 二维码的数据
		const p2 = $3;			// Logo
		const p3 = ${4:200};	// 二维码的宽
		const p4 = ${5:50};		// 二维码的宽
		const p5 = '#000000';	// 二维码的颜色
		const p6 = '#ffffff';	// 背景色
		return an21(p1, p2, p3, p4, p5, p6);
	})();